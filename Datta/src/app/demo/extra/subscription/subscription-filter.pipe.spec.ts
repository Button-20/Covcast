import { SubscriptionFilterPipe } from './subscription-filter.pipe';

describe('SubscriptionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SubscriptionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
